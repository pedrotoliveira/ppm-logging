if (project.hasProperty('release') && Boolean.parseBoolean(project.getProperty('release'))) {

    if (!hasProperty('ossrhUsername') || !hasProperty('ossrhPassword')) {
        println 'CAN\'T RELEASE PROJECT'
        return;
    }

    // Signature of artifacts
    signing {
        sign configurations.archives
    }

    println '-------------------------'
    println 'RELEASE PROJECT          '
    println 'triggered by: ' + ossrhUsername
    println '-------------------------'

    // OSSRH publication
    uploadArchives {
        repositories {
            mavenDeployer {
                // POM signature
                beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }

                repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
                    authentication(userName: ossrhUsername, password: ossrhPassword)
                }

                snapshotRepository(url: "https://oss.sonatype.org/content/repositories/snapshots/") {
                    authentication(userName: ossrhUsername, password: ossrhPassword)
                }
                
                pom.project {
                    name 'ppm-logging'
                    description 'PPM Logging Library - is a subproject from PPM Commons'
                    packaging 'jar'
                    url 'https://pedrotoliveira.github.io/ppm-commons/'
                    inceptionYear '2017'
                    scm {
                        connection 'scm:git:https://github.com/pedrotoliveira/ppm-logging.git'
                        developerConnection 'scm:git:git@github.com:https://github.com/pedrotoliveira/ppm-logging.git'
                        url 'https://github.com/pedrotoliveira/ppm-logging.git'
                    }

                    licenses {
                        license {
                            name 'GNU GENERAL PUBLIC LICENSE, Version 2'
                            url 'https://www.gnu.org/licenses/gpl-2.0.txt'							
                        }
                    }
                    developers {
                        developer {
                            id = 'pedrotoliveira'
                            name = 'Pedro Oliveira'
                            email = 'pedro.oliveira20@mail.com'
                        }
                    }                    
                }                
            }
        }
    }   
}